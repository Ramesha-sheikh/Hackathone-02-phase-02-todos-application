# Database Configuration
# Replace with your database connection string
# For PostgreSQL/NeonDB: postgresql+asyncpg://username:password@host:port/database
# For SQLite (local development): sqlite+aiosqlite:///./auth_app.db
DATABASE_URL='postgresql://neondb_owner:npg_uvEsI30zYWbG@ep-falling-term-ahc2r80a-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# Authentication Configuration
# Generate a strong secret key for JWT tokens (at least 32 characters)
AUTH_SECRET_KEY=your-super-secret-jwt-key-here-make-it-long-and-random-keep-this-safe

# CORS Configuration - Add your frontend URLs
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000", "http://localhost:3001"]

# Database Logging (set to 1 to enable SQL logging, default is 0)
DATABASE_ECHO=0

# Database Pool Configuration (default values, adjust as needed)
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10