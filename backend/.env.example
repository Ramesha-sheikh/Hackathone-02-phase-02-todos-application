# Database Configuration
# Replace with your database connection string for tasks
# For PostgreSQL/NeonDB: postgresql+asyncpg://username:password@host:port/database
# For SQLite (local development): sqlite+aiosqlite:///./todo_app.db
DATABASE_URL=postgresql+asyncpg://username:password@ep-your-endpoint.region.aws.neon.tech/yourdbname?sslmode=require

# Authentication Configuration
# This must match the AUTH_SECRET_KEY from the auth service
TODO_SERVICE_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random-keep-this-safe
# For backward compatibility
BETTER_AUTH_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random-keep-this-safe

# CORS Configuration - Add your frontend URLs
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000", "http://localhost:8001"]

# Database Logging (set to 1 to enable SQL logging, default is 0)
DATABASE_ECHO=0

# Database Pool Configuration (default values, adjust as needed)
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10